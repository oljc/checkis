---
title: isBigInt
description: 检查一个值是否为 BigInt 类型。
---

import { Callout } from 'fumadocs-ui/components/callout'

# isBigInt

检查一个值是否为 BigInt 类型。

## 语法

```typescript
isBigInt(value: unknown): value is bigint
```

## 参数

- `value` - 要检查的值

## 返回值

如果值是 BigInt 则返回 `true`，否则返回 `false`。

## 示例

### 基本用法

```typescript
import { isBigInt } from 'checkis';

// BigInt 类型
console.log(isBigInt(BigInt(123))); // true
console.log(isBigInt(BigInt('123'))); // true
console.log(isBigInt(123n)); // true
console.log(isBigInt(0n)); // true
console.log(isBigInt(-123n)); // true

// 非 BigInt 类型
console.log(isBigInt(123)); // false
console.log(isBigInt('123n')); // false
console.log(isBigInt('123')); // false
console.log(isBigInt({})); // false
console.log(isBigInt([])); // false
console.log(isBigInt(null)); // false
console.log(isBigInt(undefined)); // false
```

### TypeScript 类型守卫

```typescript
function processValue(value: unknown) {
  if (isBigInt(value)) {
    // TypeScript 现在知道 value 是 bigint 类型
    console.log(`BigInt 值: ${value}`);
    console.log(`转换为字符串: ${value.toString()}`);
    console.log(`转换为数字: ${Number(value)}`);
  } else {
    console.log('不是 BigInt 类型');
  }
}
```

### 实际应用场景

```typescript
// 大数运算验证
function safeBigIntOperation(a: unknown, b: unknown): bigint | null {
  if (isBigInt(a) && isBigInt(b)) {
    return a + b;
  }
  return null;
}

// 数据类型转换
function convertToBigInt(value: unknown): bigint | null {
  if (isBigInt(value)) {
    return value;
  }

  if (typeof value === 'number' || typeof value === 'string') {
    try {
      return BigInt(value);
    } catch {
      return null;
    }
  }

  return null;
}

// API 响应验证
interface ApiResponse {
  id: unknown;
  timestamp: unknown;
}

function validateApiResponse(response: ApiResponse) {
  if (!isBigInt(response.id)) {
    throw new Error('ID 必须是 BigInt 类型');
  }

  if (!isBigInt(response.timestamp)) {
    throw new Error('时间戳必须是 BigInt 类型');
  }

  return {
    id: response.id,
    timestamp: response.timestamp
  };
}
```

<Callout>
BigInt 是 ES2020 引入的新数据类型，用于表示任意精度的整数。它对于处理超出 JavaScript 数字安全范围的大整数非常有用。
</Callout>

## 相关函数

- [`isNumber`](/zh/api/isNumber) - 检查值是否为数字
- [`isInteger`](/zh/api/isInteger) - 检查值是否为整数
